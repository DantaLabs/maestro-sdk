{
  "input": {
    "type": "object",
    "properties": {
      "message": {
        "type": "string",
        "description": "Input message for the agent"
      }
    },
    "required": ["message"]
  },
  "output": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "description": "Status of the processing"
      },
      "message": {
        "type": "string",
        "description": "Output message from the agent"
      },
      "data": {
        "type": "object",
        "description": "Additional data from processing"
      }
    },
    "required": ["status", "message"]
  },
  "memory": {
    "type": "object",
    "properties": {
      "session_data": {
        "type": "object",
        "description": "Session-specific data storage"
      }
    }
  }
} 